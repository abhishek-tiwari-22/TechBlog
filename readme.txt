Python:
pip install -r requirements.txt

to run the code (at localhost:8000):
uvicorn main:app --reload

- can view the docs at localhost:8000/docs

----------
React:
npx create-react-app .

to start a server (at localhost:3000):
npm start